(
    (SET A '2')
    (
        IF (EQUAL A '3') (
            (PRINT "A is equal to 3")
            (PRINT "Boolean is true") 
        ) (
            (PRINT "A is not equal to 3")
            (PRINT "Boolean is false")
        )
    )
)